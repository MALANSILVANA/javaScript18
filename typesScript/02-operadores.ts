let arregloNumerosUno:number[]=[1,2,3,4,5];
let ArregloUsuarios : [
    {
        nombre: 'carlos',
        edad:28};
nombre: 'carlos',
    edad:28};
    ]

//let arregloNumerosDos:Array<number>=[1,2,3,4,5];
//funcion anonima
let sumarDosNumeros =
    (numeroUno:number, numeroDos: number): number =>{ //funciones de
    //flecha gorda
    return numeroUno + numeroDos;
    };
const usuriosCasados = arregloUsuarios
    .map()
let potenciaDeUnNumero = (numero) => {
    return numero*numero;
};
/*
let potenciaDeUnNumero = numero =>numero*numero {
* */
let sumatoriaDeValores = 0;
let resultadoForEach = arregloNumerosUno.forEach(
 (valorDelArreglo, indice, arreglo)=> {
    console.log(valorDelArreglo);
    console.log(indice);
    console.log(arreglo);
},
console.log('Resultado forEach', resultadoForEach);
console.log('sumatoria de valores', sumatoriaDeValores);
var resultadoSuma = arregloNumerosUno
    .reduce(
        (valorActualDeLaOperacion, valorDelArreglo)=>{
            console.log('valorActualDeLaOperacion',
                valorActualDeLaOperacion);
            console.log('valorDelArreglo',
                valorDelArreglo);
            return valorActualDeLaOperacion + valorDelArreglo
        },
       20
);
.some (//or
    (usuario: UsuarioArreglo)=>{
        return usuario.edad
    }
console.log('ResultadoSuma', resultadoSuma);

//.filter{
    function (usuario: UsuarioArreglo)=> {
        return (usuario.deuda <= 310);

    };
}
interface Interface {
    
}